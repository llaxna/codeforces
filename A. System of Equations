#include <iostream>
#include <cmath>
using namespace std;
 
int main() {
  int a,b,n,m;
  int count=0;
  
  cin >> n >> m;
  for (int i=0;i<=1000;i++){
    for (int j=0;j<=1000;j++){
      if (n==pow(i,2)+j && m==i+pow(j,2)){
        count++;
      }
    }
  }
  cout <<count;
}
