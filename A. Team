#include <iostream>
using namespace std;
 
int main() {
  int n,x,y,z;
  cin>>n;
 
  int s=0;
  for (int i=0;i<n;i++){
    int count=0;
    cin >> x >> y >> z;
    if (x==1)
      count++;
    if (y==1)
      count++;
    if (z==1)
      count++;
 
    if (count>=2)
     s++;
  }
  cout << s;
}
